# Logger Utility
Shared CSV logger for all demos.

Default outputs
- `logs/line_demo.csv`  → `ts_ms,event,tape_detect,note`
- `logs/imu_demo.csv`   → `ts_ms,yaw_deg,pitch_deg,roll_deg`
- `logs/ranging.csv`    → `ts_ms,true_cm,meas_cm`
- `logs/speed.csv`      → `ts_ms,v_cmd,v_est`
